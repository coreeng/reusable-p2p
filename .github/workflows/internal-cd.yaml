name: CD

on:
  push:
    branches:
      - main
      - p2p-testing

concurrency: cd-main

jobs:
  increment-version:
    uses: ./.github/workflows/internal-version.yaml
    secrets:
      git-token: ${{ secrets.GITHUB_TOKEN }}
